{
  "name": "hashtags-hub",
  "version": "1.0.0",
  "description": "A web service to get links to hashtag pages on different web platforms. The main purpose of this tool is to be used on Wikidata as the 'formatter URL' (P1630) value of 'hashtag' (P2572)",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "update-icons": "./scripts/update_icons",
    "watch": "export NODE_ENV=dev; supervisor -w server,config,public server/server.js"
  },
  "keywords": [],
  "author": "maxlath",
  "license": "AGPL-3.0",
  "devDependencies": {
    "supervisor": "^0.12.0"
  },
  "dependencies": {
    "config": "^3.2.4",
    "escape-html": "^1.0.3",
    "express": "^4.17.1"
  }
}
